for REACT frontend dependancy:
1-Installer les dépendances: npm install chart.js react-chartjs-2 lucide-react

2- Configuration Tailwind CSS: npm install -D tailwindcss @tailwindcss/vite

3- installer les dependances : npm install

then run : npm run dev


For Laravel : 

cd bechedhli-backend

composer install

cp .env.example .env  

php artisan key:generate

php artisan migrate --seed

php artisan serve --port=8000
